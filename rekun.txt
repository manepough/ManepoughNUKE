-- [[ 1. SAFE LOADSTRING & WHITELIST ]] --
local function safeLoad(url)
    local success, code = pcall(game.HttpGet, game, url)
    if not success then warn("Failed to fetch code: " .. tostring(code)) return end
    local executable, compileError = loadstring(code)
    if executable then
        local runSuccess, runError = pcall(executable)
        if not runSuccess then warn("Code ran but crashed: " .. tostring(runError)) end
    else
        warn("Syntax error in loaded code: " .. tostring(compileError))
    end
[cite_start]end [cite: 1, 2]

local whitelistedIDs = {
    [10429099415] = "FLAMEFAML",
    [9693065023] = "kupal_isme8",
}

local player = game:GetService("Players").LocalPlayer
if not whitelistedIDs[player.UserId] then
    player:Kick("Unauthorized User: You are not on the whitelist.")
    return 
[cite_start]end [cite: 2]

-- [[ 2. SERVICES & SETUP ]] --
local HttpService = game:GetService("HttpService")
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer
[cite_start]local brick = ReplicatedStorage:WaitForChild("Brick", 5) [cite: 2]

[cite_start]if makefolder then pcall(function() makefolder("WindConfigs") end) end [cite: 2]

-- [[ 3. UI ROOT & TOGGLE ]] --
local ScreenGui = Instance.new("ScreenGui", LocalPlayer:WaitForChild("PlayerGui"))
ScreenGui.Name = "WindUltimate_GodMode"
ScreenGui.ResetOnSpawn = false
[cite_start]ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling [cite: 2]

local Main = Instance.new("Frame", ScreenGui)
Main.Size = UDim2.new(0, 340, 0, 520)
Main.Position = UDim2.new(0.5, -170, 0.5, -260)
Main.BackgroundColor3 = Color3.fromRGB(15, 15, 18)
Main.BorderSizePixel = 0
Main.Visible = false
[cite_start]Instance.new("UICorner", Main).CornerRadius = UDim.new(0, 10) [cite: 4]

local MobileToggle = Instance.new("TextButton", ScreenGui)
MobileToggle.Size = UDim2.new(0, 45, 0, 45)
MobileToggle.Position = UDim2.new(0.05, 0, 0.1, 0)
MobileToggle.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
MobileToggle.Text = "MANE"
MobileToggle.TextColor3 = Color3.fromRGB(0, 255, 150)
MobileToggle.Font = Enum.Font.GothamBold
MobileToggle.TextSize = 14
[cite_start]Instance.new("UICorner", MobileToggle).CornerRadius = UDim.new(1, 0) [cite: 3, 4]

-- Draggable Toggle Logic
local toggleDrag, hasMoved = false, false
local toggleStartPos, toggleStartInput
MobileToggle.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        toggleDrag = true; hasMoved = false
        toggleStartPos = MobileToggle.Position; toggleStartInput = input.Position
    end
end)
UserInputService.InputChanged:Connect(function(input)
    if toggleDrag and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - toggleStartInput
        if delta.Magnitude > 3 then 
            hasMoved = true
            MobileToggle.Position = UDim2.new(toggleStartPos.X.Scale, toggleStartPos.X.Offset + delta.X, toggleStartPos.Y.Scale, toggleStartPos.Y.Offset + delta.Y)
        end
    end
end)
UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        if toggleDrag then toggleDrag = false; if not hasMoved then Main.Visible = not Main.Visible end end
    end
[cite_start]end) [cite: 5, 6]

-- [[ 4. MAIN CONTAINER ]] --
local Container = Instance.new("ScrollingFrame", Main)
Container.Size = UDim2.new(1, -20, 1, -55)
Container.Position = UDim2.new(0, 10, 0, 50)
Container.BackgroundTransparency = 1
Container.ScrollBarThickness = 2
Container.CanvasSize = UDim2.new(0, 0, 0, 1800)
local UIList = Instance.new("UIListLayout", Container)
UIList.Padding = UDim.new(0, 6); [cite_start]UIList.HorizontalAlignment = Enum.HorizontalAlignment.Center [cite: 7, 8]

-- [[ 5. COLOR PICKER ]] --
local PickerOverlay = Instance.new("Frame", ScreenGui); PickerOverlay.Size = UDim2.new(1, 0, 1, 0); PickerOverlay.BackgroundColor3 = Color3.new(0,0,0); PickerOverlay.BackgroundTransparency = 0.5; PickerOverlay.Visible = false; [cite_start]PickerOverlay.Active = true [cite: 9, 10]
local PickerBox = Instance.new("Frame", PickerOverlay); PickerBox.Size = UDim2.new(0, 220, 0, 280); PickerBox.Position = UDim2.new(0.5, -110, 0.5, -140); PickerBox.BackgroundColor3 = Color3.fromRGB(25, 25, 30); [cite_start]Instance.new("UICorner", PickerBox).CornerRadius = UDim.new(0, 10) [cite: 11]
local WheelImage = Instance.new("ImageLabel", PickerBox); WheelImage.Name = "Wheel"; WheelImage.Size = UDim2.new(0, 160, 0, 160); WheelImage.Position = UDim2.new(0.5, -80, 0, 20); WheelImage.Image = "rbxassetid://6020299385"; [cite_start]WheelImage.BackgroundTransparency = 1 [cite: 12, 13]
local SelectorDot = Instance.new("Frame", PickerBox); SelectorDot.Size = UDim2.new(0, 18, 0, 18); SelectorDot.AnchorPoint = Vector2.new(0.5, 0.5); SelectorDot.BackgroundColor3 = Color3.new(1,1,1); SelectorDot.BackgroundTransparency = 0.15; SelectorDot.BorderSizePixel = 3; SelectorDot.BorderColor3 = Color3.new(0.1,0.1,0.1); Instance.new("UICorner", SelectorDot).CornerRadius = UDim.new(1,0); [cite_start]SelectorDot.Visible = false [cite: 14]
local RGBDisplay = Instance.new("TextLabel", PickerBox); RGBDisplay.Size = UDim2.new(1, 0, 0, 20); RGBDisplay.Position = UDim2.new(0, 0, 0, 190); RGBDisplay.Text = "255, 255, 255"; RGBDisplay.TextColor3 = Color3.new(1,1,1); RGBDisplay.Font = Enum.Font.GothamBold; RGBDisplay.TextSize = 14; [cite_start]RGBDisplay.BackgroundTransparency = 1 [cite: 15, 16]
local ClosePickerBtn = Instance.new("TextButton", PickerBox); ClosePickerBtn.Size = UDim2.new(0.8, 0, 0, 35); ClosePickerBtn.Position = UDim2.new(0.1, 0, 1, -45); ClosePickerBtn.BackgroundColor3 = Color3.fromRGB(0, 150, 80); ClosePickerBtn.Text = "CONFIRM"; ClosePickerBtn.Font = Enum.Font.GothamBold; ClosePickerBtn.TextColor3 = Color3.new(1,1,1); [cite_start]Instance.new("UICorner", ClosePickerBtn) [cite: 17, 18]

local currentTargetButton, currentHue, currentSat, lastColorUpdate = nil, 0, 0, 0
local WHEEL_RADIUS = 80

local function updateColorMath(input)
    local now = os.clock()
    if now - lastColorUpdate < 0.015 then return end
    lastColorUpdate = now
    local center = WheelImage.AbsolutePosition + (WheelImage.AbsoluteSize / 2)
    local delta = Vector2.new(input.Position.X, input.Position.Y) - center
    local dist = delta.Magnitude; local sat = math.clamp(dist / WHEEL_RADIUS, 0, 1)
    local rawAngle = math.atan2(delta.Y, delta.X); local hue = (rawAngle + math.pi / 2) / (math.pi * 2)
    if hue < 0 then hue = hue + 1 end; hue = hue % 1
    local c = Color3.fromHSV(hue, sat, 1)
    if currentTargetButton then 
        currentTargetButton.BackgroundColor3 = c; 
        RGBDisplay.Text = string.format("%d, %d, %d", math.round(c.R*255), math.round(c.G*255), math.round(c.B*255)) 
    end
    if dist > 2 then 
        SelectorDot.Position = UDim2.new(0.5, (delta.Unit * (sat * WHEEL_RADIUS)).X, 0.5, (delta.Unit * (sat * WHEEL_RADIUS)).Y); SelectorDot.Visible = true 
    else SelectorDot.Visible = false end
[cite_start]end [cite: 19, 20, 21, 22, 23]

WheelImage.InputBegan:Connect(function(input) 
    if not (input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch) then return end
    updateColorMath(input)
end)
[cite_start]ClosePickerBtn.MouseButton1Click:Connect(function() PickerOverlay.Visible = false end) [cite: 24]

local function openPicker(btn) 
    currentTargetButton = btn; local c = btn.BackgroundColor3; 
    RGBDisplay.Text = string.format("%d, %d, %d", math.round(c.R*255), math.round(c.G*255), math.round(c.B*255)); 
    PickerOverlay.Visible = true 
[cite_start]end [cite: 25, 28]

-- [[ 6. PAINTER DATA ]] --
local faces = {"Front", "Back", "Top", "Bottom", "Right", "Left"}
local faceData = {}
local nameToId = { ["Top"] = 0, ["Bottom"] = 1, ["Back"] = 2, ["Front"] = 3, ["Right"] = 4, ["Left"] = 5 }

for _, name in ipairs(faces) do
    [cite_start]local row = Instance.new("Frame", Container); row.Size = UDim2.new(0.95, 0, 0, 35); row.BackgroundTransparency = 1 [cite: 29]
    local txt = Instance.new("TextBox", row); txt.Size = UDim2.new(0.75, 0, 0, 30); txt.BackgroundColor3 = Color3.fromRGB(30, 30, 35); txt.Text = "GG'S"; txt.PlaceholderText = name .. " Text"; txt.TextColor3 = Color3.new(1, 1, 1); txt.Font = Enum.Font.Gotham; txt.TextSize = 12; [cite_start]Instance.new("UICorner", txt) [cite: 30, 31, 32]
    local clrBtn = Instance.new("TextButton", row); clrBtn.Size = UDim2.new(0.2, 0, 0, 30); clrBtn.Position = UDim2.new(0.8, 0, 0, 0); clrBtn.BackgroundColor3 = Color3.fromRGB(255, 0, 0); clrBtn.Text = ""; [cite_start]Instance.new("UICorner", clrBtn) [cite: 33, 34]
    clrBtn.MouseButton1Click:Connect(function() openPicker(clrBtn) end)
    faceData[name] = {txtBox = txt, clrBtn = clrBtn}
end

local function createBtn(text, color)
    local btn = Instance.new("TextButton", Container); btn.Size = UDim2.new(0.95, 0, 0, 35); btn.BackgroundColor3 = color; btn.Text = text; btn.Font = Enum.Font.GothamBold; btn.TextColor3 = Color3.new(1,1,1); Instance.new("UICorner", btn); return btn
[cite_start]end [cite: 35, 36]

-- [[ 7. STABLE EXECUTION LOGIC ]] --
local useToxic, autoAnchor, spamming = true, true, false

local function instantMobileExecute()
    local Character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
    local tool = LocalPlayer.Backpack:FindFirstChild("Paint") or Character:FindFirstChild("Paint")
    if not tool then warn("Paint tool not found!") return end
    [cite_start]if tool.Parent ~= Character then Character:WaitForChild("Humanoid"):EquipTool(tool); task.wait(0.5) end [cite: 37]

    local remote = tool:FindFirstChild("Event", true) or tool:FindFirstChildWhichIsA("RemoteEvent", true)
    local rootPart = Character:FindFirstChild("HumanoidRootPart")
    if not remote or not brick or not rootPart then return end
    
    local rootPos = rootPart.Position
    local key = "both \u{1F91D}"
    local PURE_BLACK = Color3.fromRGB(0, 0, 0)

    -- Base Toxic Layer
    remote:FireServer(brick, Enum.NormalId.Top, rootPos, key, PURE_BLACK, useToxic and "toxic" or "plastic", "")
    [cite_start]task.wait(0.6) [cite: 38]

    -- Numeric Side Loop (Fixes Painter)
    for sideNum = 0, 5 do
        local faceName
        for name, id in pairs(nameToId) do if id == sideNum then faceName = name break end end
        local uiConfig = faceData[faceName]
        if uiConfig then
            local sideEnum = Enum.NormalId:GetEnumItems()[sideNum + 1] 
            remote:FireServer(brick, sideEnum, rootPos, key, uiConfig.clrBtn.BackgroundColor3, "spray", uiConfig.txtBox.Text)
            task.wait(0.3)
        end
    [cite_start]end [cite: 39, 40]

    [cite_start]if autoAnchor then remote:FireServer(brick, Enum.NormalId.Top, rootPos, "material", PURE_BLACK, "anchor", "") end [cite: 41]
end

-- [[ 8. BUTTON ACTIONS ]] --
local MaterialBtn = createBtn("Material: TOXIC", Color3.fromRGB(120, 0, 200))
MaterialBtn.MouseButton1Click:Connect(function() useToxic = not useToxic; MaterialBtn.Text = "Material: " .. (useToxic and "TOXIC" or "PLASTIC") end)

local AnchorBtn = createBtn("Auto-Anchor: ON", Color3.fromRGB(0, 120, 200))
AnchorBtn.MouseButton1Click:Connect(function() autoAnchor = not autoAnchor; AnchorBtn.Text = "Auto-Anchor: " .. (autoAnchor and "ON" or "OFF") end)

local RunBtn = createBtn("EXECUTE SEQUENCE", Color3.fromRGB(0, 150, 100))
RunBtn.Size = UDim2.new(0.95, 0, 0, 45)
RunBtn.MouseButton1Click:Connect(instantMobileExecute)

local DelCubesBtn = createBtn("NUKE CUBES", Color3.fromRGB(200, 50, 0))
local DestroyerBtn = createBtn("BKIT DESTROYER (CAREFUL)", Color3.fromRGB(220, 20, 20))
DestroyerBtn.Size = UDim2.new(0.95, 0, 0, 50); DestroyerBtn.TextSize = 18

-- [[ 9. SLOTS & SAVING ]] --
local SlotHeader = Instance.new("TextLabel", Container); SlotHeader.Size = UDim2.new(0.95, 0, 0, 30); SlotHeader.Text = "--- CONFIG SLOTS ---"; SlotHeader.TextColor3 = Color3.new(0.6, 0.6, 0.6); SlotHeader.Font = Enum.Font.GothamBold; SlotHeader.TextSize = 14; [cite_start]SlotHeader.BackgroundTransparency = 1 [cite: 43]
local SlotNameBox = Instance.new("TextBox", Container); SlotNameBox.Size = UDim2.new(0.95, 0, 0, 35); SlotNameBox.BackgroundColor3 = Color3.fromRGB(30, 30, 35); SlotNameBox.Text = ""; SlotNameBox.PlaceholderText = "Type Slot Name Here..."; SlotNameBox.TextColor3 = Color3.new(1,1,1); [cite_start]Instance.new("UICorner", SlotNameBox) [cite: 44, 45]
local SaveSlotBtn = createBtn("SAVE NEW SLOT", Color3.fromRGB(70, 70, 80))
local SlotFrame = Instance.new("Frame", Container); SlotFrame.Size = UDim2.new(0.95, 0, 0, 0); SlotFrame.AutomaticSize = Enum.AutomaticSize.Y; SlotFrame.BackgroundTransparency = 1; [cite_start]Instance.new("UIListLayout", SlotFrame).Padding = UDim.new(0, 4) [cite: 46, 47]

local function toHex(c) return string.format("#%02X%02X%02X", c.R*255, c.G*255, c.B*255) end
[cite_start]local function fromHex(hex) hex = hex:gsub("#",""); return Color3.fromRGB(tonumber("0x"..hex:sub(1,2)), tonumber("0x"..hex:sub(3,4)), tonumber("0x"..hex:sub(5,6))) end [cite: 48]

SaveSlotBtn.MouseButton1Click:Connect(function()
    local name = SlotNameBox.Text ~= "" and SlotNameBox.Text or "Unnamed_Slot"
    local data = {anc = autoAnchor, tox = useToxic, faces = {}}
    for _, n in ipairs(faces) do data.faces[n] = {t = faceData[n].txtBox.Text, c = toHex(faceData[n].clrBtn.BackgroundColor3)} end
    if writefile then writefile("WindConfigs/"..name..".json", HttpService:JSONEncode(data)) end
end)

-- [[ 10. NUKE CUBES & DESTROYER ]] --
DelCubesBtn.MouseButton1Click:Connect(function()
    local char = LocalPlayer.Character
    local deleteTool = char:FindFirstChild("Delete") or LocalPlayer.Backpack:FindFirstChild("Delete")
    if deleteTool and deleteTool.Parent ~= char then char.Humanoid:EquipTool(deleteTool); task.wait(0.45) end
    local remote = deleteTool:FindFirstChild("Event", true) or deleteTool:FindFirstChildWhichIsA("RemoteEvent", true)
    if remote then
        local rootPos = char.HumanoidRootPart.Position
        for i = 1, 300 do
            pcall(function() remote:FireServer(nil, rootPos + Vector3.new(0, 10000, 0)) end)
            task.wait(0.04)
        end
    end
[cite_start]end) [cite: 66, 68, 69]

DestroyerBtn.MouseButton1Click:Connect(function()
    local char = LocalPlayer.Character
    local deleteTool = char:FindFirstChild("Delete") or LocalPlayer.Backpack:FindFirstChild("Delete")
    if deleteTool and deleteTool.Parent ~= char then char.Humanoid:EquipTool(deleteTool); task.wait(0.2) end
    local scriptEvent = deleteTool and deleteTool:FindFirstChild("Script") and deleteTool.Script:FindFirstChild("Event")
    if scriptEvent then scriptEvent:FireServer(brick, char.HumanoidRootPart.Position) end
[cite_start]end) [cite: 70]